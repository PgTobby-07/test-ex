[build]
builder = "RAILPACK"
buildCommand = "python -m pip install --upgrade pip && python -m pip install -r requirements.txt && python -m pip install cryptography"

[deploy]
startCommand = "bash start.sh"